name: Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test-bash:
    name: Bash (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
      - uses: actions/checkout@v7.0.0

      - name: Run Bash suite, including launch contracts
        shell: bash
        run: bash ./tests/test_aagent.sh

  test-powershell:
    name: PowerShell (windows-latest)
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v7.0.0

      - name: Run PowerShell suite, including launch contracts
        shell: pwsh
        run: ./tests/test_aagent.ps1
